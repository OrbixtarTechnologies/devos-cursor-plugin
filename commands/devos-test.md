---
name: devos-test
description: Design and run proportionate verification for the current change.
---
Use test-engineering. Inspect the project's existing test stack first. Add regression coverage when needed and run the smallest meaningful set of checks before broader checks. Delegate to `devos-test` when a specialist pass is useful.
