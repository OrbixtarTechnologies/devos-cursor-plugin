# Marketplace Readiness

Before publishing, replace placeholder homepage/repository URLs with the public source repository, confirm maintainer identity, and run the current Cursor plugin review checklist.

Current package properties:
- Cursor Plugin manifest under `.cursor-plugin/plugin.json` with name `devos` and display name DevOS
- Rules, skills, agents, commands, hooks, and MCP included
- Local validation script included
- No embedded credentials
- External integrations documented as optional and read-only by default
